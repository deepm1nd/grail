# CHANGELOG

> **For human reference only. Agents do not need to read this file.**
>
> This file consolidates the full version history extracted from all grail instruction
> files at v0.6.3. Each section corresponds to one source file. Prior history entries
> are preserved verbatim. The v0.6.3 entry in each section describes what changed in
> that batch; individual files now carry only a single pointer: "See CHANGELOG.md."

---

## Grail Project Version History

> **Overall version for the grail workflow itself** (distinct from any individual
> project's own version, and distinct from the per-file tracking below). Starts at
> v0.9.4 — prior batches (v0.6.3 through v0.9.3) are not retroactively assigned an
> overall version here; only the detailed per-file sections below cover that history.
> Each entry summarizes, at a glance, everything a given grail version batch touched;
> the per-file sections below remain the authoritative, detailed record of exactly what
> changed in each file.

| Version | Date | Files touched | Summary |
|---------|------|----------------|---------|
| 0.10.1 | 2026-07-22 | `MAINTENANCE.md`, `maintenance_batch_template.md`, `maintenance_checklist_template.md`, `maintenance_prompt_template.md`, `DEVELOPMENT.md`, `development_checklist_template.md`, `dev_prompt_template.md`, `CI.md`, `README_template.md` | **Maintenance workflow restructured M1–M4 → M0–M4**: every item now starts with Claude (no intake-time Track A/B guess); M0 is interactive Elicitation with dual user confirmation (completeness + approval); M1 is a 5-question mechanical Impact Triage deciding lightweight (straight to Jules, provisional SemVer stated at M1 exit) vs. full (User Story generated in real `US-[DOMAIN]-NNN` numbering, continues to M2→M3→M4); no file is presented to the user before M3 (full path) or Jules-artifact generation (lightweight path). Jules escape valve (existing §12 mechanism) now explicitly covers a lightweight item found to have real impact. Batch/Checklist/Prompt filenames carry their real target version from batch-open — `_open` placeholder/rename-at-release lifecycle retired, since batch composition is fixed at open time (no drip-feed); sole exception is a rare escalation-driven mid-batch correction. Added §6a — Regression Scaffold canonical definition: Rust (`test_<nnnn>__description`, nextest-filterable) and Playwright (`TEST-<nnnn>: description`, `-g`-filterable) naming conventions connecting an abstract Test ID to an actual invocable test; retrofit policy for missing/partial traceability tables (rename-in-place default, wrapper-shim only as a logged `dev_risks.md` exception); detailed required-behavior spec for a project-specific `scripts/verify_traceability.js` (authored per-project during Development Phase, not grail-supplied) that mechanically confirms every traceability row's test exists, runs, and passes. Added Appendix G — single merged Readiness Audit + Remediation prompt, replacing the previous two separate prompts, with a hard stop presenting the gap report (one finding per checklist item, explicitly distinguishing "missing entirely" vs. "exists but incomplete" for item 1) and requiring an explicit per-finding decision before any remediation begins. Added an explicit Phase-boundary scope rule (`MAINTENANCE.md` §10, mirrored in `DEVELOPMENT.md` §5.2 step 8 and both prompt templates' Session-End Checklists): a session checks only its own current phase's Exit Criteria, never the next phase's Entry Criteria — that belongs entirely to the session that opens the next phase. README badges migrated from shields.io `dynamic/toml` (silently fails on private repos — unauthenticated fetch, no auth mechanism available) to CI-written static badges: `README_template.md`'s 7 badges (6 metrics + Branch Status) now marker-wrapped (`<!-- BADGE:X:START/END -->`) with `pending`/`lightgrey` placeholders; `CI.md` Stage 6 gained a detailed required-behavior spec (field mapping, escaping, missing-file fallback) for a project-specific `scripts/metrics/write_readme_badges.js` (authored per-project, not grail-supplied) plus a "Write README badges" step before its commit step, which now `git add`s `README.md` alongside `metrics/*.toml`; the obsolete per-phase "update badge URLs to current Branch Name" requirement removed from `DEVELOPMENT.md` §5.2, `development_checklist_template.md` (5 references removed, including the entire `DOC-PHASE-BADGE` recurring task — not 1 as originally scoped), and confirmed absent from `development_plan_template.md`. |
| 0.9.4 | 2026-07-19 | `DEVELOPMENT.md`, `DESIGN.md`, `CI.md`, `PREFERRED_TOOLS.md`, `agents/exemplars/development_plan_template.md`, `agents/exemplars/development_checklist_template.md`, `agents/exemplars/architecture_specification_template.md`, `agents/exemplars/README_template.md`, `agents/ESP32_ESPIDF_RUST_BUILD_GUIDE.md` | Productization Readiness Checklist (9 items, `DEVELOPMENT.md` §5.2.4) and its corresponding `PROD-NNN` Final Phase tasks; Requirement-to-Test traceability table as a standing artifact; Appendix B (Bug Fix Log) and Appendix F (Spec Amendments) added to the Architecture Specification template; phase-level Exit Criteria strengthened to require a full CI-equivalent sequence with findings fixed, not just per-task; `CI.md` corrected three times — artifact actions pinned to `@v5`, a `permissions: contents: write` block added to `metrics_commit`, a close-out step-ordering bug fixed (`Evaluate required gates` now runs before the branch/status marker write, so `job.status` is accurate), and `continue-on-error: true` added to every genuinely gated Stage 1–5 step (deliberately excluding Stage 0 and `Evaluate required gates` itself) so the gate-aggregation step can actually see every stage's outcome instead of stopping at the first failure; `cargo binstall` established as the preferred install mechanism for cargo-installable tools, with a new whole-script idempotency mandate for `setup_env.sh`. Retrofit materials for bringing an in-flight project up to this version are a separate, non-versioned one-off (not tracked here). |

---

## AGENTS.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.0.0 | — | Claude | Baseline: five-phase lifecycle overview; Autonomous/Advisory operating modes; Core Mandates (work/filesystem integrity, protocol adherence, quality/completeness, behavioral caution and simplicity, naming conventions); User Interaction (formal approval protocol, CHAT: protocol, synchronous gated execution with unconditional one-item-at-a-time elicitation). No version number or revision history prior to this entry; baseline reconstructed for record-keeping. |
| 1.1.0 | 2026-06-20 | Claude | Added explicit Rust-only scope statement. Added Stable Identifier Assignment and Explicit Backtracking to §2.1. Made §3.3 Iterative Elicitation mode-conditional. Tightened §2.5 naming-convention rationale. |
| 0.6.1 | 2026-06-24 | Claude | Catch-up: §1.5.3 renamed to "Autonomy Toggle and Named Presets." Added "No cross-session memory" note to §1.5.2. Added handoff note manifest requirement. Added file versioning note. |
| 0.6.2 | 2026-06-24 | Claude | Added "Re-Verify Before Extending a Rule by Analogy" to §2.4.1. Added delivered-files versioning acknowledgment to §1.5.2. |
| 0.6.3 | 2026-06-29 | Claude | **v0.6.3 batch.** Reduced lifecycle to Design + Development only (Verification/Release/Maintenance phases removed). Added §2.6 Missing-Tools Mandate (self-install + append to setup_env.sh/bat, escalation still fires). Added §2.7 No-Copy-Checklist Mandate. Added §2.8 One-Phase-Per-Session Mandate. CHAT: protocol moved to its own §3.2.1 with explicit cross-reference from §3.2. All V/R/M phase references removed. §1 lifecycle updated to two phases. Paths corrected to reflect repo layout (CLAUDE.md + AGENTS.md in root; agents/ and agents/exemplars/). CHANGELOG.md created; Appendix R replaced with pointer. |
| 0.7.0 | 2026-07-02 | Claude | **v0.7.0 batch.** §1.5.2 Advisory Mode: removed dangling reference to never-created `agents/ADVISORY_MODE_GUIDE.md`; replaced with a generic pointer to the project's own working-arrangement file (e.g. `CLAUDE.md`), which is read unconditionally regardless of any pointer here. No other content change — this file remains lifecycle-agnostic to the CLAUDE.md-side Step 2/Step 9/Asset Manifest changes landing in the same batch. |
| 0.7.1 | 2026-07-03 | Claude | §2.7 extended: beyond the existing no-copy/no-rename rule, now states explicitly that the Development Checklist is the *only* documentation/planning file a Development Phase session may edit at all (every other doc file read-only, an inconsistency in one is an Escalation Trigger, never a same-session fix), and that edits within the Checklist are scoped to the session's own current Phase only (never another phase's marks, never structural text). Operationalized in `agents/DEVELOPMENT.md` §4. |
| 0.8.5 | 2026-07-04 | Claude | **v0.8.5 batch.** §2.6 rewritten: a missing tool/prerequisite that installs cleanly still proceeds automatically; any install failure or resulting conflict now stops the session per the Development Phase escalation model rather than escalating-and-continuing. §3.1 tightened: exact, all-uppercase `APPROVED` only — "Continue"/"Proceed"/"Go ahead" removed as substitutes, for a Step Gate, Minor Change, Major Change, and handoff-package preparation alike; added the distinct `BACKTRACK APPROVED` / `SURGICAL FIX OVERRIDE` tokens (`CLAUDE.md` §3.6.1). §2.1's Explicit Backtracking mandate gained the Surgical Fix Override cross-reference. |
| 0.8.6 | 2026-07-06 | Claude | **v0.8.6 batch.** §2.1 Mandatory Artifact Preservation rewritten: removed `target/` from the commit mandate; now explicitly distinguishes non-reproducible evidence artifacts (committed — `test_outs/`, screenshots, coverage reports) from reproducible build outputs (gitignored — `target/`, `dist/`); cross-references `development_plan_template.md` §3 for the required `.gitignore` entries. |
| 0.8.7 | 2026-07-06 | Claude | **v0.8.7 batch (part 1).** §2.2 gained two new mandates: **Selective Reading Mandate** (whole-file reads of large documents explicitly forbidden; all documentation reads must use targeted extraction via `sed`/`grep`/`awk`; cross-references `dev_prompt_template.md` reference table) and **No Broad Repository Scan Mandate** (speculative directory/file enumeration at session start or any other point explicitly forbidden; navigation to specific known paths only). |
| 0.8.7 | 2026-07-07 | Claude | **v0.8.7 batch (part 2) — work-preservation overhaul.** §2.1 rewritten: task-level `submit` cadence (via new `agents/AGENT_TOOL_POLICY.md`) is now the concrete preservation mechanism, replacing implicit phase-only checkpointing — bounds crash blast-radius to one task/sub-task. New WIP Checkpoint mechanism: a `submit` not claiming DoD, exempt from Error-Free-Builds, always bypassing code review, prefixed `[WIP-CHECKPOINT]`, mandatorily describing what was attempted/confirmed/incomplete so a resuming session can pick up in place. §2.2 gained a **Code Review Policy: BYPASSED** bullet (flip-by-deletion) and a cross-reference to the new `agents/AGENT_TOOL_POLICY.md`. §2.8 reworked: One-Phase-Per-Session generalized to a per-phase-declared **Session Unit** (`Phase`/`Task`/`Code+Verify`), changeable only via Plan-Change Escalation. |
| 0.8.8 | 2026-07-08 | Claude | **v0.8.8 batch.** §2.1 Task-Level Submit Cadence strengthened: submit immediately upon DoD completion — no reading ahead, no cleanup pass, no continuing "while it's fresh" before that submit fires; DoD-satisfied-but-unsubmitted is treated as incomplete. §2.1 Mandatory Artifact Preservation repointed from `test_outs/` to the new `test/[projectname]_phase_[N]_verification.md` + `test/phase_[N]/` layout (`development_plan_template.md` §11.4). §2.3 gained a new **No Compressed Formats** bullet: every phase/task/section/list in a delivered file must be written out in full — no "repeat this block," no ellipsis-for-omitted-items, no other placeholder shorthand — operationalized concretely in `development_checklist_template.md` and `DESIGN.md` §5.8. §2.7 rewritten: Checklist edits are now explicitly **bracket-content-only** — flipping a mark in an existing `[ ]`, checking a `Submitted` box, or appending a Session Log row; rewording, commentary, restructuring, or touching another phase's marks is out of scope and an Escalation Trigger. |
| 0.8.9 | 2026-07-09 | Claude | **v0.8.9 batch.** §2.1 gained an explicit scope note: the Selective Reading Mandate and No Broad Repository Scan Mandate (§2.2) govern Development Phase (Jules) sessions only — a Design Phase (Claude) session reads as broadly as synthesis requires and is not bound by either. Addresses observed Claude sessions self-reporting compliance with a mandate that was never meant to apply to them. |
| 0.8.11 | 2026-07-09 | Claude | **v0.8.11 batch.** §2.1 Explicit Backtracking exception renamed **Surgical Fix Override -> Post-Audit Fix Pass** (token `SURGICAL FIX OVERRIDE` -> `POST AUDIT FIX`); note added that the fix pass covers every audit finding plus anything discovered incidentally, never left flagged-but-unfixed. §3.1 decision-token list updated to match. §2.2 Visual & Media Asset Input cross-references `CLAUDE.md` §3.7 new persistent per-step mockup ask, Authority Levels, and the auth/role addendum (introduced this batch). §2.3 Sole Responsibility and §2.4.1 Stop on Confusion or Gates both gained a cross-reference to `agents/DEVELOPMENT.md` §4's new Ask-on-Uncertainty split, reconciling this generic ask-when-unsure mandate with the Development Phase's own Escalation Trigger stop model. |
| 0.9.0 | 2026-07-12 | Claude | **v0.9.0 batch.** §2.2's Technology and Tools bullet gained `agents/CI.md` alongside the existing PREFERRED_* references. §2.6 Missing Tools mandate gained an explicit note that its install-once framing assumes a persistent session/environment (a Development Phase session, potentially backed by a Jules Environment Snapshot) — a CI run has no such persistence, so `agents/CI.md`'s Stage 0 restates the install step as unconditional on every run rather than a one-time check, and this file's §2.6 defers to that file for CI-specific handling. |
| 0.9.1 | 2026-07-14 | Claude | **v0.9.1 batch.** §2.3's Maximal Implementation bullet strengthened with an explicit in-task, real-time version of the anti-stub mandate -- no `todo!()`/`unimplemented!()`/placeholder-return/partial-branch left "to come back to," even mid-session -- rather than relying solely on the retroactive Phase-End Quality Assurance review (§2.2) to catch it; that review is now stated explicitly as a final backstop, not the primary enforcement point. |

---

## CLAUDE.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.5.0 | 2026-06-24 | Claude | Full restructure for clarity and token efficiency — no intended behavior change. Consolidated propose-with-flagged-assumptions principle into §3.1; Grouped Closing Protocol into §3.2; selectable-options convention into §3.3; reduced 8-step description to per-step deltas. Added §3.7 Tailored Mode. Replaced relative cross-references with stable named anchors. Squashed prior 0.1.0–0.4.0 history. |
| 0.6.0 | 2026-06-24 | Claude | Real new behavior. Step 1 always-on high-rigor research. §3.3 research trigger independent of decision importance. §4 step-number filename convention for intermediate/handoff artifacts. §3.7 redesigned as general per-step toggle with two presets. New §3.8 visual input. New §3.9 no unilateral technical sufficiency. New §3.10 "not enough—another pass" mechanism. Step 8 Phase Sizing complexity-scoring formula. |
| 0.6.1 | 2026-06-24 | Claude | Clarified session-per-step reality. New §3.11 on file delivery for session boundaries. Handoff notes must enumerate every file. Input verification against handoff manifest. |
| 0.6.2 | 2026-06-24 | Claude | Selectable-options three-layer presentation precondition. Four research trigger categories. Grouped Closing Protocol bulk-reply loophole closed. §3.8 visual input as discovery source. §3.11 pre-handoff staleness check and version-suffix scheme. Development Checklist no version suffix. New Step 8 kickoff prompt deliverable. Phase Summary §11.3 and §13.1 escalation formalized. |
| 0.6.3 | 2026-06-29 | Claude | **v0.6.3 batch.** §1 updated: session-per-step is now a hard rule stated explicitly. New §3.12 Step 7 Backtrack Workflow (findings always require reopening originating steps; all fixes in one backtrack session, earliest-to-latest, ending with package for new Step 7 session). Handoff note naming standardized: `[projectname]_design_handoff_stepN_passX.md`. §3.11 pre-handoff staleness check extended to verify file naming convention compliance. §4 file naming updated to new `[projectname]_artifact_NN_topic_vN.md` pattern (projectname-first, 00-based sequence). Phase Summary naming updated to `[projectname]_phaseN_summary.md`. V/R/M phase references removed. CHAT: protocol cross-referenced. CHANGELOG.md created; Appendix replaced with pointer. |
| 0.7.0 | 2026-07-02 | Claude | **v0.7.0 batch — major restructure.** Step 2 Pass A/B split folded into a single session (no more `passA`/`passB` handoff notes). Architecture Specification regrouped from 4 topic-based files to **8 files, one per originating Design Step 1–6** (§4.1), fixing both unbounded file size at scale (50+ stories/100–200+ requirements) and shared-version-bump churn across unrelated content. New §4.3 makes per-file independent, session-end-only version bumping explicit, corrects a prior example that implied a single shared `v[N]` across the file set, and explicitly extends "touched this session" to files outside the current step (backtrack/revisit cases) after observed cases of that bump being skipped. New §3.13 End-of-Step Open-Items Review: every gate now re-surfaces the full cumulative Deferred/Remaining-Ambiguous register from all prior steps, not just the current step's own batch. New §2.2/§3.8 Visual & Media Asset Input mechanism: HTML presumptively authoritative for structure/behavior, images treated as a design-token source (branding/iconography/typography/palette/tiles), audio/video tracked-only; new Asset Manifest record with immutable filenames and fixed `assets/{html,images,audio,video}/` repository paths shared verbatim between Design and Development. §3.11 handoff notes now require an explicit `## Next Action: Start Step N[, Pass X].` directive as the first line, after observed sessions reporting no explicit ask and offering the obvious next step as a question instead of simply starting it. §3.4 Step 8 row references the new Frontend Targeted Interleaving phase-sequencing principle (full mechanism in `development_plan_template.md`/`architecture_specification_template.md`). Stale `_01_overview`/"Concept Mapping"/"Requirements List" examples throughout §3.11 corrected to match the new per-Step file identity. |
| 0.7.1 | 2026-07-03 | Claude | **Verbosity pass — no intended behavior change.** Rewrote prose throughout for concision (repeated emphasis/rationale collapsed, redundant restatements removed); all mandates, tables, ID schemes, filenames, and gate conditions preserved verbatim in substance. ~16% smaller by size. |
| 0.8.0 | 2026-07-03 | Claude | §3.8 gains an explicit exception to §3.11's file-delivery rule: user-supplied UI/media assets are never re-packaged into a handoff note — only referenced by filename; a step needing an asset requests the current version directly from the user; Spec/Plan content states the asset "will be available in `assets/...` at Development Phase" rather than assuming the Design-time copy is final. Asset Manifest table itself still carries forward normally. §3.11 cross-references this exception. `[projectname]_dev_prompt.md` renamed to `[projectname]_dev_agent_prompt.md` throughout (source template also renamed, see its own CHANGELOG section). |
| 0.8.5 | 2026-07-04 | Claude | **v0.8.5 batch — major restructure.** Removed the Autonomy Toggle/Tailored/Full-Autonomous presets and the Grouped Closing Protocol/propose-with-flagged-assumptions/Selectable-Options mechanism entirely (former §3.1-3.3, §3.7). Replaced by one fixed procedure for every step but 7 and 9 (new §3.1-3.2): RCD (Research, Competitive research, Draft) generates the core batch research-backed and in full; bulk-accept closes the great majority; RATS (Research, Analysis, Table, Selectable-options) resolves every residual item to exactly one of Resolved / Deferred-to-a-specific-step / Future Feature / Rejected. Step 2 always runs in full, no skip path. New §3.6.1 Surgical Fix Override (`SURGICAL FIX OVERRIDE` vs. `BACKTRACK APPROVED`), a compressed single-session alternative to a full backtrack, still mandatorily ending in a fresh audit session when triggered by a Step 7/9 finding. §3.10 (formerly §3.11) File Delivery reworked: interim files use a lowercase-letter suffix (`v3a`, `v3b`), never version-bumped or packaged until exact `APPROVED`. §3.12 (formerly §3.13) Open Items Register updated for the four terminal outcomes. All sections from former §3.7 onward renumbered down by one. Step 8 now also drafts the project README. Step 7/9 both gain a terminal-RATS-outcome audit check. Renamed `dev_agent_prompt_template.md`/`[projectname]_dev_agent_prompt.md` back to `dev_prompt_template.md`/`[projectname]_dev_prompt.md` throughout. |
| 0.8.7 | 2026-07-07 | Claude | **v0.8.7 batch — Step 8 row.** Noted that per-task Design Refs and Submit Points, and per-phase Session Unit, are populated at Step 8 drafting time, not left as stubs; Phase Sizing formula note that a Code/Verify-split task counts as 2. |
| 0.8.9 | 2026-07-09 | Claude | **v0.8.9 batch.** §1 gained an explicit note that `AGENTS.md` §2.1's Selective Reading/No Broad Repository Scan Mandates do not bind this (Design Phase) session. §4.3 gained a mandatory **bump-then-fix-then-verify** ordering rule: version bump happens first, cross-link rewriting happens against post-bump filenames second, and a final reference-consistency grep-pass runs last immediately before packaging — closing the observed failure mode where fix-then-bump silently re-staled references the same session corrected them. §3.11 (Step 7 Backtrack Workflow): opening the backtrack session now proceeds directly into fixing by default (the user starting the session is itself the go-ahead, no separate "proceed with fixes" needed); new step 7, a **two-consecutive-non-clean-pass circuit breaker** — a second straight non-clean Step 7 run triggers a Convergence Diagnostic (recurring vs. genuinely new findings) and an explicit user choice (Surgical Fix Override / accept as documented deferred item / manually-scoped fix) rather than an unbounded third identical pass. §3.12 Open Items Register redefined as **open-items-only**: Resolved/Future Feature/Rejected entries are now deleted from the Register immediately on reaching their terminal outcome (folded into Spec content / Spec §2.6 / the retired-ID record respectively) rather than kept as a permanent historical record; only not-yet-reached Deferred items remain on the Register. Step 7/9 table rows (§3.4) updated to check "Register contains only valid Deferred items" instead of "every entry has a terminal outcome." |
| 0.8.10 | 2026-07-09 | Claude | **v0.8.10 batch.** §4.3 gained a new rule, distinct from the existing filename cross-link rule: **inline prose citations to another file's content never include a version number** — `File N §section` only, never `File N vM`. Addresses a repeated multi-session defect where `File N vM`-style prose citations drifted out of sync across bumps happening in sessions that never touched the citing file, with each backtrack session catching only the drift present at that moment while falsely asserting an exhaustive re-sweep (the v0.8.9 "final reference-consistency pass" rule, while correctly closing the *same-session* fix-then-bump gap, didn't address this *cross-session* drift, since it only re-derives during a session actually touching a file). Removing the version number from prose citations entirely eliminates the defect at its source rather than requiring an ongoing sweep to keep one current. |
| 0.9.0 | 2026-07-12 | Claude | **v0.9.0 batch.** §3.4 Step 8 table row's Deliverable column extended to include draft `ci.yml` and draft `THIRD_PARTY_LICENSES.md`, alongside the existing draft README/`.gitignore`; description column notes both are generated from Step 5's new CI Stage Applicability and License Disclosure Artifact findings respectively, and reviewed at Development Phase 0 (`agents/DEVELOPMENT.md` §5.2.2/§5.2.3) rather than authored from scratch there. |

---

## agents/DESIGN.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.0.01 | 2025-10-10 | Jules | Initial creation. |
| 0.0.02 | 2025-10-10 | Jules | Complete overhaul to enforce rigor. |
| 0.0.03 | 2025-10-12 | Jules | Integrated iterative 6-step workflow. |
| 0.2.00 | 2025-10-12 | Jules | DESIGN v2.0: 8-Step Gated Protocol. |
| 0.2.01 | 2025-10-13 | Jules | Explicit 'STOP and Ask' mandates. |
| 0.3.00 | 2026-06-19 | Claude | Generalized propose-with-flagged-assumptions into Steps 1, 3, 4, 5. Step 2 mode-conditional elicitation. Notation-judgment flagging in Step 6. Environment/configuration elicitation sub-step in Step 8. §2.1 Multi-File Packaging. |
| 0.3.01 | 2026-06-20 | Claude | Step 7 independence framing added. Rust-only scope statement in §1. Step 5 dependency-feasibility language tightened. |
| 0.5.0 | 2026-06-24 | Claude | Mechanical-vs-substantive principle consolidated into §5 opening. §5.9 Autonomy Toggle/Named Presets acknowledged. 18% shorter. |
| 0.6.0 | 2026-06-24 | Claude | Step 1 always-on high-rigor research. Phase Sizing Mandate updated with complexity-scoring formula. §5.9 pointer broadened. |
| 0.6.3 | 2026-06-29 | Claude | **v0.6.3 batch.** §5 opening: each step begins in its own new session stated explicitly. §5.7 Step 7 updated: clean report → proceed; any finding → trigger backtrack workflow (defined in CLAUDE.md §3.12). Step 7 audit now includes file-naming compliance check. Handoff note naming standard referenced throughout. §2.1 multi-file packaging updated to new filename pattern. V/R/M refs removed. CHANGELOG.md pointer added. |
| 0.7.0 | 2026-07-02 | Claude | **v0.7.0 batch.** Lifecycle extended from 8 to **9 steps**: new §5.9 Step 9 (Plan & Checklist Audit) — independent, adversarial audit of the Development Plan/Checklist against the finalized Architecture Specification and against `development_plan_template.md` §15's own DoD, mirroring Step 7's independence; findings classify as (A) Plan/Checklist-only (reopen Step 8 alone) or (B) Spec-originating (reopen the relevant Spec step and re-clear Step 7 before returning to Step 8). §6 Phase Completion Criteria now requires both Step 7 and Step 9 clean audits, not just artifact delivery. §5.2 Step 2 folds Pass A/B into one session. §5.1 Step 1 broadens asset solicitation to HTML/images/brand assets/audio/video, cross-referencing `CLAUDE.md` §3.8. §5.8 Step 8 adds the Frontend Targeted Interleaving phase-sequencing principle. §2 Goal section notes both audits as completion prerequisites. Autonomy toggle section renumbered to §5.10 and updated: Step 9 can never be autonomous, same rationale as Step 7. |
| 0.7.1 | 2026-07-03 | Claude | §5.1 Step 1 row's asset cross-reference to `CLAUDE.md` §3.8 clarified: assets are never packaged into a Design-Phase session handoff note, only referenced by filename — a later step needing one requests the current version from the user directly. `dev_agent_kickoff_prompt_template.md` references updated to renamed `dev_agent_prompt_template.md`. |
| 0.8.5 | 2026-07-04 | Claude | **v0.8.5 batch.** §5's shared-mechanism intro replaced with the fixed RCD/RATS procedure description. Removed §5.10 Autonomy Toggle entirely. Step 2 always runs in full. Step 7/9 gain the terminal-RATS-outcome audit check and a Surgical Fix Override cross-reference. Step 8 gains README drafting as an explicit deliverable. Template filename references updated to `dev_prompt_template.md`. |
| 0.8.7 | 2026-07-07 | Claude | **v0.8.7 batch.** §5.8 Step 8 gained: per-task Design Refs and Submit Points, and per-phase Session Unit, are populated at drafting time, not left as stubs. |
| 0.8.8 | 2026-07-08 | Claude | **v0.8.8 batch.** §5.7 (Step 7) and §5.9 (Step 9) Output lines rewritten: the Audit Report is presented in chat at the gate and is not a standalone required file; on any finding, its content is embedded directly in the backtrack handoff note rather than delivered separately (`CLAUDE.md` §3.10/§3.11). §5.8 Checklist bullet gained an explicit no-placeholder/expand-in-full reminder (`AGENTS.md` §2.3 No Compressed Formats). |
| 0.8.9 | 2026-07-09 | Claude | **v0.8.9 batch.** §5.7 (Step 7) Process rewritten into two explicit tiers: **Tier A — Fixed Checklist** (story/requirement mapping, atomicity, file naming, Open Items Register empty-except-valid-Deferred, content continuity, anti-stub — the audit's actual deterministic anchor) and **Tier B — Bounded Exploratory Pass** (one freeform "blind spot" review per audit, findings folded into a project-specific Tier A addendum rather than rediscovered blind each pass) — addresses observed non-convergence across independent Step 7 passes. §5.7/§5.9 gate-behavior text updated: opening the backtrack session proceeds directly into fixing by default (no separate "proceed with fixes" needed); both gained a **circuit breaker** note mirroring `CLAUDE.md` §3.11 step 7 — a second consecutive non-clean pass triggers a Convergence Diagnostic and explicit user choice instead of an unbounded third identical pass. Open Items Register check in both §5.7 and §5.9 updated from "every entry has a terminal outcome" to "contains only valid, not-yet-reached Deferred items," matching `CLAUDE.md` §3.12's redefinition. |
| 0.8.10 | 2026-07-09 | Claude | **v0.8.10 batch.** §5.7 Tier A gained a new bullet: no inline prose citation to another file may include a version number (`File N vM` form is itself a finding; correct to `File N §section`), mirroring `CLAUDE.md` §4.3's new rule. |
| 0.8.11 | 2026-07-09 | Claude | **v0.8.11 batch.** §5.1 Step 1 sentence trimmed to a pointer at `CLAUDE.md` §3.7's now-authoritative persistent per-step mockup ask, Authority Levels, Proactive UI-Impact Flagging, and auth/role addendum (moved there to avoid duplicating the mechanism in two files). §5.8 Step 8 Phase Sizing Mandate: Complexity Score is now a mandatory, always-shown Phase Index column. §5.7 (Step 7) and §5.9 (Step 9) gate-behavior rewritten with the new **Trivial/Substantive severity tier** (`CLAUDE.md` §3.11): an all-Trivial audit result is fixed in place, same session, no backtrack; only a Substantive finding triggers the full backtrack/**Post-Audit Fix Pass** (renamed from Surgical Fix Override, token `POST AUDIT FIX`) workflow, which now explicitly fixes incidentally-discovered defects alongside the audit's own findings rather than leaving them flagged for a future auditor; circuit breakers on both steps now count consecutive Substantive-bearing passes only. §5.9 Process gained a Complexity Score recompute check. |
| 0.8.12 | 2026-07-11 | Claude | **v0.8.12 batch.** §5.8 Step 8 gained a new `.gitignore` deliverable bullet — drafted at Design Step 8 per `development_plan_template.md` §3's depth-agnostic skeleton, reviewed/extended at Development Phase 0, mirroring the README convention; Output and GATE lines updated to include it. §5.9 Step 9 gained an explicit note that `.gitignore` is not a Step 9 audit input — its correctness is confirmed at Development Phase 0 review instead. §6 Phase Completion Criteria updated to require `.gitignore` among the committed approved artifacts. |
| 0.9.0 | 2026-07-12 | Claude | **v0.9.0 batch — CI formalization.** §5.5 Step 5 feasibility check extended: (1) identifies which conditional CI stages apply for the project (WASM/Trunk, Playwright/E2E, ESP32, infra services) as new `agents/CI.md`-skeleton inputs, no file produced; (2) direct proposed dependencies are now checked against `PREFERRED_DEPENDENCIES.md`'s license criterion (permissive/notice-only) as an extension of the existing Preferred/Forbidden/Requires-Approval match — explicitly does not cover transitive dependencies, which have no resolvable `Cargo.lock` yet at Design time; (3) identifies that a `THIRD_PARTY_LICENSES.md` disclosure artifact is required, fed by `cargo-deny`'s license enumeration. §5.8 Step 8 gained four new deliverables generated from Step 5's findings: `.github/workflows/ci.yml` (from the `agents/CI.md` skeleton), `scripts/metrics/` parser scripts (drafted alongside `ci.yml` since its Stage 2/3/5/6 steps invoke them, no dependency-resolution prerequisite of their own), `LICENSE.md` (static PolyForm Noncommercial 1.0.0 text, final at Step 8 — no reconciliation needed, unlike `THIRD_PARTY_LICENSES.md`), and initial `THIRD_PARTY_LICENSES.md` content — all added to Step 8's Output list and GATE alongside Plan/Checklist/Dev Prompt/README/`.gitignore`. §5.9 Step 9 gained two new mechanical checks: (1) a generated `ci.yml` referencing `check_env.sh` without a preceding `setup_env.sh` step is a Substantive finding; (2) any GitHub Action pinned below the current Node-major requirement (e.g. `actions/checkout@v4` when `v5`+ is current) is a Trivial finding, fixed in place — added after this was observed to recur across generated projects despite `PREFERRED_TOOLS.md` already documenting the pinning rule, indicating a one-time generation-time reminder alone is insufficient (see `agents/CI.md`'s v0.9.0 entry for the underlying root cause, GitHub Actions' per-job runner isolation, that this check and Stage 5's Action-based rewrite together fix). Note added that `.gitignore`/`LICENSE.md`/`scripts/metrics/`/`THIRD_PARTY_LICENSES.md` are not Step 9 audit inputs, each confirmed by its own separate mechanism instead. §6 Phase Completion Criteria updated to include `ci.yml`, `scripts/metrics/`, `LICENSE.md`, and `THIRD_PARTY_LICENSES.md` among the committed approved artifacts. |
| 0.9.1 | 2026-07-14 | Claude | **v0.9.1 batch.** Step 7 Tier A checklist gained a Data-Dictionary-vs-Requirement completeness pass: every Data Dictionary entry (Step 6 Information Viewpoint) must trace to a requirement referencing it, and every requirement naming/implying a data structure must have a corresponding Data Dictionary entry; a mismatch either direction is a finding. Step 8 gained a Mandatory Pre-Submit Local Verification requirement: every task-complete Submit Point drafted into the Plan must include, as its final DoD item, running the full local CI-equivalent sequence (Lint & Format, Build, Test, Coverage, Security Scan) and confirming all clean before Submit -- standing on top of, not a substitute for, that task's own narrower Verification Method. Step 9 gained a matching mechanical check: a Submit Point missing this DoD item is a Trivial finding, fixed in place. Step 8 also gained a new CHANGELOG.md deliverable, generated from the new `agents/exemplars/CHANGELOG_template.md`, added to the Output list, GATE, and Phase 6 Completion Criteria alongside the existing artifact set. |
| 0.9.2 | 2026-07-16 | Claude | **v0.9.2 batch.** Step 7 Tier A checklist gained a new permanent mechanical item: every Core requirement ID (Spec §3) must have ≥1 trace to an exit criterion in §9.2, same no-judgment-call framing as the other Tier A items. Step 8 gained a new deliverable: an initial, empty/skeleton `docs/[project_name]_dev_risks.md`, generated from the new `agents/exemplars/dev_risks_template.md` alongside `CHANGELOG.md` — the durable home for standing risks discovered during Development Phase (deny.toml `ignore`/`[[licenses.exceptions]]` entries, advisory-driven dependency swaps, approved `[patch]` exceptions), distinct from the Architecture Specification's Risk Management section, which covers only Design-Phase-identified risks. Output list, GATE, and Step 9's not-an-audit-input note all updated to include the new file. |
| 0.9.3 | 2026-07-19 | Claude | **v0.9.3 batch.** §5.8's Environment/Configuration Elicitation bullet gained: (1) explicit elicitation of the repository's GitHub org and repo name, needed to populate `[org]`/`[repo]` in the README's badge URLs — no longer left as an unresolved placeholder; (2) a pointer to `PREFERRED_TOOLS.md`'s new binary-install preference, so `setup_env.sh` content is drafted binary-first from the start rather than only self-correcting later via the Missing Tool Protocol. |
| 0.9.4 | 2026-07-19 | Claude | **v0.9.4 batch.** Step 5 gained a Productization Applicability finding (determines whether `PROD-007`–`PROD-009` apply, feeding Step 8). Step 8 gained: a Requirement Traceability Table Skeleton deliverable (`test/[projectname]_requirement_traceability.md`); an instruction to draft one `PROD-NNN` task per applicable Productization Readiness Checklist item into the Final Phase; both added to the Output/GATE lists. Step 9 gained mechanical check #5 (a Final Phase missing an applicable `PROD-NNN` task is a Trivial finding) and the traceability table added to the not-an-audit-input note. |

---

## agents/DEVELOPMENT.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.0.01 | 2025-10-10 | Jules | Initial creation and refactoring. |
| 0.6.3 | 2026-06-29 | Claude | **v0.6.3 batch.** §4 added: Missing-Tools Mandate (self-install + idempotent append to scripts/setup_env.sh and scripts/setup_env.bat; escalation trigger still fires). No-Copy-Checklist Mandate. One-Phase-Per-Session Mandate. §3 updated to reference `[projectname]_dev_prompt.md`. §5.2 README task added (Phase 0 scaffold + final-phase review). Docker/deploy guidance added (heavy services via docker-compose in deploy/; reference PREFERRED_SERVICES.md). "Verification Phase" wording replaced with "feature-complete." V/R/M refs removed. CHANGELOG.md pointer added. |
| 0.7.0 | 2026-07-03 | Claude | §4's No-Copy-Checklist Mandate extended into an explicit **exclusive-file-touch rule**: `[projectname]_dev_checklist.md` is the only documentation/planning file a development-agent session may edit — every other doc file (Architecture Spec, Plan, Kickoff/Dev-Agent Prompt, handoff notes, Phase Summaries) is read-only, with a stray inconsistency treated as an Escalation Trigger rather than patched in passing. Edits within the checklist are further scoped to box-flipping/Session-Log rows for the agent's **current Phase only** — never marks belonging to another phase, never structural text. `[projectname]_dev_prompt.md` renamed to `[projectname]_dev_agent_prompt.md` throughout. |
| 0.8.5 | 2026-07-04 | Claude | **v0.8.5 batch.** Missing-Tool/Escalation mandate rewritten to Stop-Summarize-Wait: a cleanly-installing missing tool is the sole case continuing without stopping; every other unresolvable issue now stops the entire session immediately — write Phase Summary, no PR, no partial continuation. §5.2.1 reworked: Design Step 8 drafts the README; Development's Phase 0 task is review/confirm/enhance. Reverted naming to `dev_prompt_template.md`/`[projectname]_dev_prompt.md`. |
| 0.8.7 | 2026-07-06 | Claude | **v0.8.7 batch (part 1).** §5.2 step 1 Session-Start Sequence rewritten to reflect lean reading model: reads checklist, phase summaries, current phase's plan section (§6.1 + §8), and protocols file upfront using targeted extraction; explicitly prohibits reading all Architecture Spec files or all Dev Plan files upfront; prohibits broad repository scan; Architecture Spec and remaining plan sections referenced on demand only via `dev_prompt_template.md` reference table. |
| 0.8.7 | 2026-07-07 | Claude | **v0.8.7 batch (part 2) — work-preservation overhaul.** §4 gained the WIP-Checkpoint exception to Error-Free Builds. New §5.1 Mandatory Code/Verify Split (a/b sub-tasks, derived from Verification Method) and Submit Point cadence (submit at every declared checkpoint, resumed via plain "Continue"/"Proceed"). §5.2 step 2 gained the explicit three-way task-state check (not-started / WIP-checkpointed-resume-in-place / complete) against declared Submit Points — the direct fix for a crash mid-build-test-debug losing sibling-task work. Step 7 narrowed to phase-level wrap-up only; step 8 generalized to "next Session Unit." Cross-referenced new `agents/AGENT_TOOL_POLICY.md`. |
| 0.8.8 | 2026-07-08 | Claude | **v0.8.8 batch.** §1 Introduction now states Development Phase sessions are Jules-based (Design Phase is Claude-based), cross-referencing `README.md`. §5.2 step 1 gained mandatory phase-branch checkout (create/resume the phase's declared Branch Name) before any code is touched. §5.2 step 3 gained the Verification-file-append step, run immediately on each task's DoD satisfaction, before the Checklist update and submit. |
| 0.8.11 | 2026-07-09 | Claude | **v0.8.11 batch.** §4 gained a new **Ask-on-Uncertainty, Scoped Below Escalation Severity** mandate, reconciling `AGENTS.md` §2.3/§2.4.1's generic "ask well-formed questions" rule with this file's own Escalation Trigger model: task-level implementation detail is asked directly in-session via `message_user`/`request_user_input` and the same session continues; anything rising to Plan/Spec-level ambiguity (§13 Escalation Triggers) is never resolved by asking and continuing — it stops the entire session as before. When genuinely unsure which tier applies, the Escalation Trigger tier is the default. |
| 0.8.12 | 2026-07-11 | Claude | **v0.8.12 batch.** §5.2 step 1's branch-checkout sentence strengthened: the phase Branch Name must now be re-confirmed before starting each subsequent task in the session, not just once at session start — a session finding itself on the wrong branch mid-task stops and corrects it before further code changes. §5.2 step 3 gained an explicit exact-Checklist-order, no-skip rule: tasks are worked in the order listed, no reordering, no skipping ahead, no leaving a DoD sub-item unchecked-but-passed-over, without the user's explicit permission given that session — an unnecessary-seeming or already-satisfied task/item is a question or Escalation Trigger, never a silent skip. |
| 0.9.0 | 2026-07-12 | Claude | **v0.9.0 batch — CI formalization.** New **§5.2.2 CI Workflow** and **§5.2.3 Third-Party License Disclosure** sections, mirroring §5.2.1's README pattern: `ci.yml` is drafted at Design Step 8, Phase 0 reviews/confirms/extends it; `THIRD_PARTY_LICENSES.md` is drafted at Step 8 but necessarily provisional (no `Cargo.lock` existed yet), so Phase 0 creates `deny.toml` (no prerequisite of its own, but naturally created alongside the workspace scaffold) and runs the first real `cargo deny check licenses` against the actual resolved `Cargo.lock`, reconciling the file against it — any violation found is an Escalation Trigger, not a silent fix. `LICENSE.md`'s Phase 0 review is a simple accuracy check only (static text, no reconciliation needed). §5.2's Documentation bullet updated to reference the new subsections and `deny.toml` creation. Cross-references `agents/CI.md`'s Stage 0 (Environment Setup): CI has no Jules-style Environment Snapshot equivalent, so `scripts/setup_env.sh` must run fresh, explicitly, ahead of any tool-assuming step. |
| 0.9.1 | 2026-07-14 | Claude | **v0.9.1 batch.** §5's Pre-Commit Verification & Quality Assurance step gained a new **Mandatory Pre-Submit Local Verification** bullet ahead of the existing Documentation/Assurance Review bullets: every task-complete Submit Point now runs the full local CI-equivalent sequence, with the Security Scan's `cargo deny`/`cargo audit` run explicitly stated as read-only -- any license/advisory finding is an immediate stop-and-alert, never resolved by the session itself editing `THIRD_PARTY_LICENSES.md` (that file's sole legitimate writer remains CI's own Stage 5 regeneration step). §5.2.3's `THIRD_PARTY_LICENSES.md` description gained an explicit lightweight-format note (one-line description + dependency/license table only, no prose, no full license text) applying at both the Step 8 draft stage and every later regeneration, so Phase 0's reconciliation is a table expansion, not a restructuring. |
| 0.9.3 | 2026-07-19 | Claude | **v0.9.3 batch.** §5.2 step 1's branch-checkout sentence gained an explicit exact-Branch-Name mandate: the declared Branch Name is used verbatim, never with an appended numeric hash, timestamp, or session identifier (observed failure: `phase12b-4334437132416834814` checked out instead of the Plan's actual declared name). §5.2 step 5's Documentation bullet now requires every phase, not just first/final, to update the README's shields.io badge URLs to the current phase's Branch Name and re-confirm `[org]`/`[repo]` substitution — metrics commit per-branch, so a badge left pointing at a prior phase's branch (or at `main`) goes stale. |
| 0.9.4 | 2026-07-19 | Claude | **v0.9.4 batch.** New §5.2.4, Productization Readiness Checklist — nine items (Regression Scaffold, Versioning, No Incomplete/Stubbed Work, User-Facing Docs, Developer-Facing Docs, License/Dependency Drift, and three conditional items: Rollback Procedure, Operational Runbook, Monitoring Baseline), the single canonical definition referenced by both the Final Phase's Exit Criteria and `agents/MAINTENANCE.md`'s future Readiness Audit. §5.2 step 4 (Phase Integration and System Test) strengthened: every phase's Exit Criteria now include re-running the full local CI-equivalent sequence at the phase level (not just per-task) and resolving/fixing any finding before Exit Criteria are satisfied — previously only a build+test pass with log inspection. Documentation bullet (step 5) now cross-references §5.2.4 for the final phase. |

---

## agents/AGENT_TOOL_POLICY.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.8.7 | 2026-07-07 | Claude | **New file.** Resurrects and updates the Allowed/Requires-Approval/Forbidden tool-tiering content from a prior, now-repurposed file (tool names updated to current platform vocabulary: `submit`, `replace_with_git_merge_diff`, etc.). Forbidden tier retains the two-step "propose → `ARE YOU SURE?`" confirmation ritual. Cross-references `AGENTS.md` §2.1 (reset/restore prohibition) and §2.2 (Code Review Policy) rather than duplicating either. States explicitly that post-`submit` session pause is resumed via plain "Continue"/"Proceed," distinct from `AGENTS.md` §3.1's `APPROVED` token. |

---

## agents/RUST_PREFERENCES.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.1.0 | 2026-06-19 | Claude | Initial creation. Naming/keyword constraints, type-system/ownership constraints, WASM concurrency constraints. |
| 0.1.1 | 2026-06-19 | Claude | COOP/COEP blocking trigger. Ranked recommended-crates table. Step 5 summary updated. |
| 0.2.0 | 2026-06-20 | Claude | Crate table reordered to lead with gloo-worker as default. |
| 0.6.3 | 2026-06-29 | Claude | **v0.6.3 batch.** §0 added: Rust 2024 edition mandate; MSRV-at-Step-5 policy; dual-MSRV pattern for publishable library crates; pre-flight version sanity check. cargo-llvm-cov replaces cargo-tarpaulin as coverage tool. Reference to PREFERRED_TOOLS.md added. CHANGELOG.md pointer added. |
| 0.8.5 | 2026-07-04 | Claude | Added a cross-reference from the Coverage Tooling note to `agents/ESP32_ESPIDF_RUST_BUILD_GUIDE.md` for embedded ESP32/ESP-IDF components. |
| 0.8.8 | 2026-07-08 | Claude | No content change this batch. |
| 0.8.11 | 2026-07-09 | Claude | §0 gained a **preferred floor of 1.94.0** for the computed workspace MSRV — the version pre-installed in the dev agent environment. Preference, not a mandate: if Step 5's computed max-of-dependencies value is at or below 1.94.0, record 1.94.0 instead (no cost, absorbs minor future creep); a higher computed value still wins as before. |

---

## agents/PREFERRED_DEPENDENCIES.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.6.3 | 2026-06-29 | Claude | **v0.6.3 batch (initial versioned entry).** Rust 2024 edition mandate added. cargo-tarpaulin removed (replaced by cargo-llvm-cov in PREFERRED_TOOLS.md). trunk removed (moved to PREFERRED_TOOLS.md). webpack removed entirely. MSRV policy pointer added. CHANGELOG.md pointer added. |
| 0.7.0 | 2026-07-02 | Claude | Added Embedded (ESP32/ESP-IDF) section: `esp-idf-hal`, `esp-idf-svc`, `esp-camera-rs`, with a Step 5 MSRV/toolchain note (separate `espup`-managed track, independent of workspace/WASM MSRV). Applies primarily to embedded Rust projects. |
| 0.8.5 | 2026-07-04 | Claude | Added `btleplug`, `rumqttc`, `aes-gcm`, `tokio-cron-scheduler`, `totp-rs`, `qrcode`, `image`, `wiremock`, `wasmtime`/`wasmtime-wasi`/`wit-bindgen`, `embedded-graphics`/`ssd1306`/`mipidsi`. Added a Dependencies Requiring Approval section for vendor/integration-specific crates seen in a prior engagement. Cross-referenced `agents/ESP32_ESPIDF_RUST_BUILD_GUIDE.md`. |
| 0.8.8 | 2026-07-08 | Claude | `rand` and `uuid` flagged with a cross-reference to `PREFERRED_TOOLS.md`'s new `getrandom`/`wasm_js` backend config — required for either dependency to compile on `wasm32-unknown-unknown`, discovered via a real CI failure (`getrandom` 0.3's backend-selection breaking change). |
| 0.8.11 | 2026-07-09 | Claude | **v0.8.11 batch.** New **No Local Patching, Forking, or Vendoring** section: a Cargo `[patch]` section, a vendored/forked crate copy, or a local `path =` override standing in for a published version are forbidden by default, same approval tier as an Unlisted dependency (`AGENTS.md` §2.2) — never a silent workaround for a bug, missing feature, or MSRV/edition mismatch. Rare exception (unreleased upstream fix, no viable alternative) requires explicit approval, an exact pinned commit hash (never a moving branch/tag), and a recorded justification/tracking issue in the Architecture Spec's Risk Management section and `CHANGELOG.md`, revisited and removed once a real release resolves it. |
| 0.9.0 | 2026-07-12 | Claude | **v0.9.0 batch.** New explicit **license-compatibility criterion** stated ahead of the dependency lists: every listed dependency must be permissive and notice/attribution-only (no share-alike, no copyleft, no obligation beyond preserving the dependency's own license notice) — matches the project's default output license (PolyForm Noncommercial) and the `cargo-deny` `[licenses]` allow-list now in `PREFERRED_TOOLS.md`. Future additions to any tier are evaluated against this criterion, not copy-pasted onto the list. "Dependencies Requiring Approval" section gained an explicit instruction: approval must include a license-compatibility check against this criterion, since a Requires-Approval dependency's own transitive tree is exactly where an incompatible license (e.g., MPL-2.0, LGPL) is most likely to slip in unnoticed (observed case: `yansi` via `leptos_macro`'s transitive tree). |
| 0.9.1 | 2026-07-14 | Claude | **v0.9.1 batch.** License Compatibility Criterion satisfying-list extended: added CC0-1.0, Zlib, NCSA, and BSL-1.0 (Boost Software License -- explicitly distinguished from the non-permissive Business Source License, which does not satisfy this criterion). No dependency removed or reclassified this batch; PolyForm Noncommercial remains the project's own output license only, never a dependency-allow-list entry. |
| 0.9.2 | 2026-07-16 | Claude | **v0.9.2 batch.** License Compatibility Criterion section gained new `[[licenses.exceptions]]` guidance: a specific third-party dependency whose license doesn't satisfy the criterion but is accepted as a narrow, per-crate exception belongs in `deny.toml`'s `[[licenses.exceptions]]`, never the global `[licenses]` allow-list (which has no workspace/transitive scope filter); same approval tier as an Unlisted dependency, same standing-risk logging convention as an `[advisories].ignore` entry. No Local Patching section's `[patch]`-exception logging location repointed: justification and tracking issue now recorded in the new `docs/[project_name]_dev_risks.md` (`agents/exemplars/dev_risks_template.md`), not the Architecture Specification's Risk Management section — this is a Development-Phase discovery, not a Design-Phase-identified risk, and the two documents have separate change-control regimes. |

---

## agents/PREFERRED_TOOLS.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.1.0 | 2026-06-29 | Claude | Initial creation. Covers: cargo-nextest, cargo-llvm-cov, cargo-deny, cargo-audit, cargo-watch, trunk, sqlx-cli, cargo-expand, wasm-pack, protoc. Includes rust-toolchain.toml template and pre-flight version check guidance. |
| 0.7.0 | 2026-07-02 | Claude | Added Embedded section: `espflash` (flashing/serial-monitor tool for ESP32/ESP-IDF targets). |
| 0.8.5 | 2026-07-04 | Claude | Merged in the Forbidden Tools section and non-fatal-tools guidance from `PREFERRED_TOOLS_proposed.md` v0.2.0. Added explicit Trunk `dist/` vs. project `assets/` disambiguation with a `Trunk.toml` pin example. Missing Tool Protocol updated to Stop-Summarize-Wait for anything beyond a clean install. Cross-referenced `agents/ESP32_ESPIDF_RUST_BUILD_GUIDE.md`. |
| 0.8.8 | 2026-07-08 | Claude | **v0.8.8 batch — three real CI failures resolved.** New WASM subsection: `getrandom` 0.3+'s wasm backend requires both a `Cargo.toml` feature (`wasm_js`) and a `.cargo/config.toml` `rustflags` cfg — either alone is insufficient; documented with the exact snippets. **cargo-deny** now requires an explicit version pin (was unpinned `--locked` only), plus a known-good `deny.toml` skeleton — an unpinned install let a schema-breaking `cargo-deny` release (`unmaintained`/`unsound` changed from lint-level strings to dependency-scope filters) invalidate an already-approved config with no warning until CI failed. New **GitHub Actions** section: pin actions to the current Node major at minimum (Node 24 as of this writing, e.g. `actions/checkout@v5`) to avoid the recurring Node-deprecation warning on hosted runners. |
| 0.8.11 | 2026-07-09 | Claude | Rust Toolchain section's MSRV line cross-references `RUST_PREFERENCES.md` §0's new preferred floor of 1.94.0 (dev agent environment default) — used whenever the computed value would otherwise be lower; a higher computed value still wins. |
| 0.9.0 | 2026-07-12 | Claude | **v0.9.0 batch.** `deny.toml` skeleton gained a `[licenses]` allow-list (previously absent — cargo-deny's default-deny behavior meant the skeleton as written rejected virtually every real dependency tree, including plain MIT/Apache-2.0): `MIT`, `Apache-2.0`, `Apache-2.0 WITH LLVM-exception`, `BSD-2-Clause`, `BSD-3-Clause`, `ISC`, `Unicode-3.0`/`Unicode-DFS-2016` — see `PREFERRED_DEPENDENCIES.md`'s new license-compatibility criterion for the rationale. New **Canonical Commands** table added: one row per tool (`cargo-nextest`, `cargo-llvm-cov`, `cargo-deny`, `cargo-audit`, Playwright) mapping each to its CI-invocation form and the `metrics/*.toml` file it feeds, cross-referencing `agents/CI.md`. Missing Tool Protocol gained an explicit CI cross-reference: `scripts/setup_env.sh` must be invoked as its own CI step (Stage 0, `agents/CI.md`) — CI has no Jules-style Environment Snapshot, so nothing installed by a prior session carries over; a CI job assuming tools are already present without its own `setup_env.sh` call is a defect, not an environment quirk. Apt-based install steps in `scripts/setup_env.sh` now required to be defensive regardless of environment: explicit `sudo add-apt-repository -y universe` followed by a second `apt-get update` before any `universe`-component package install, guarding against package-index/mirror drift (observed failure: `libusb-1.0-dev` unavailable on a GitHub Actions run despite `universe` nominally being enabled by default). **Also this batch:** the GitHub Actions section's Node-major pinning rule generalized from a single `actions/checkout` example to an explicit per-action table (`actions/checkout@v5`, `actions/cache@v5`), after the identical Node-20-deprecation warning recurred on `actions/cache@v4` despite `checkout` already being called out — this is a property of each action's own packaged runtime, not a one-time fix. |
| 0.9.1 | 2026-07-14 | Claude | **v0.9.1 batch.** New standing top-of-file mandate: Python is forbidden across the entire workflow (Design, Development, CI, all scripts), not scoped to any one domain -- exception only via explicit one-off user approval when no Node.js/bash option is viable. Script-language preference stated for cases with a choice: Node.js preferred, bash+jq acceptable for simple/low-branching cases. cargo-deny pin bumped 0.16 -> 0.19; `deny.toml` skeleton rewritten for the 0.19.x schema -- the `[advisories]` lint-level fields (unmaintained/unsound/yanked/vulnerability/notice) no longer exist at all as of this schema generation (fully removed, not just changed meaning, since the prior 0.8.8 entry documented the deprecation) -- replaced with scope-only `unmaintained`/`unsound` settings and an `ignore` allowlist; `[licenses]` allow-list gained CC0-1.0/Zlib/NCSA/BSL-1.0 to match `PREFERRED_DEPENDENCIES.md`'s same-batch update. New explicit clippy strictness policy: `-D warnings` mandatory fleet-wide; `-D clippy::unwrap_used` is a per-project Design-Step-5 opt-in, not a default, given its friction against legitimate test-code `.unwrap()` usage. |
| 0.9.2 | 2026-07-16 | Claude | **v0.9.2 batch.** New **Frontend Visual Testing (Playwright)** section: standardized idempotent `setup_env.sh` install-guard block (non-fatal, the one place `\|\| true` handling belongs for Playwright), illustrative `test/playwright/package.json`/`playwright.config.js` shapes, `test/playwright/tests/*.spec.js` convention, and a `.gitignore` node_modules-anywhere rule — actual config content is project-specific, set at Design Step 5, never hardcoded. `deny.toml` skeleton gained `[licenses.private]` (`ignore = true`, excludes private/unpublished workspace crates from the license check — their `publish = false` marking is now a mechanical scaffold default per `development_plan_template.md` §3) and a `[[licenses.exceptions]]` example block, cross-referencing the new per-crate-exception guidance in `PREFERRED_DEPENDENCIES.md`. `[advisories].ignore` policy's standing-risk logging repointed from the Architecture Specification's Risk Management section to the new `docs/[project_name]_dev_risks.md` (`agents/exemplars/dev_risks_template.md`) — a Development-Phase discovery, not a Design-Phase-identified risk. Added a worked-examples subsection under the ignore-list policy (proc-macro-only transitive crates, load-bearing-engine transitive crates with real runtime surface, and the `bincode` ignore-vs-swap dual-disposition case) as reference material. |
| 0.9.3 | 2026-07-19 | Claude | **v0.9.3 batch.** GitHub Actions pinning table gained `actions/upload-artifact@v5` and `actions/download-artifact@v5` rows, so these don't silently drift to a stale `@v4` pin by a future session copying an old workflow. Missing Tool Protocol gained a new standing preference: install a prebuilt binary release over a source build (`cargo install`) wherever one exists for the environment's platform, falling back to source builds only when no binary release exists — applies both at Design-time `setup_env.sh` drafting and later self-installs. |
| 0.9.4 | 2026-07-19 | Claude | **v0.9.4 batch.** Missing Tool Protocol's binary-preference rule sharpened: for any Rust/`cargo`-installable tool, prefer `cargo binstall <crate>` specifically over plain `cargo install` — a single, consistent, cargo-native mechanism rather than an ad hoc install method per tool; `cargo-binstall` itself installed once, early, via its own install script, not via `cargo install cargo-binstall` (which would defeat the point). New standing mandate: `setup_env.sh`/`.bat` must be idempotent **as a whole script**, re-run end-to-end against an already-provisioned environment, not merely idempotent line-by-line — verified whenever the script is modified, not just the newly-appended line. |

---

## agents/PREFERRED_SERVICES.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.1.0 | 2026-06-29 | Claude | Initial creation. Approved services: PostgreSQL+SQLx, Neo4j, Qdrant, MinIO, Redis. Docker-compose preferred; configs in deploy/. Port conventions. Service snippet templates. HTTP-over-HTTPS preference for local dev. |
| 0.7.0 | 2026-07-02 | Claude | Added Mosquitto (MQTT broker) as an approved service, with compose snippet and a note that Mosquitto 2.x requires an explicit `mosquitto.conf` (unlike this file's other services) since it refuses anonymous connections and binds no listener by default. No MQTT client crate is yet listed in `PREFERRED_DEPENDENCIES.md` — flagged as Requires-Approval until one is explicitly added. |
| 0.8.5 | 2026-07-04 | Claude | `rumqttc` approved in `PREFERRED_DEPENDENCIES.md` — removed the prior placeholder in the Mosquitto section. |

---

## agents/exemplars/architecture_specification_template.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.1.00 | — | — | Initial creation: Solution Strategy, Security Architecture/STRIDE, Rust-Specific Conventions, promoted Technology Stack, ADRs, Risks & Technical Debt, Implementation Roadmap, Configuration Reference, Quality Attribute Scenarios, 9-criteria/Rust-smell self-check tables. |
| 0.1.01 | 2026-06-20 | Claude | Fixed stale filename reference. Updated traceability note. |
| 0.2.00 | 2026-06-20 | Claude | Added §6.5 CI/CD Pipeline. Added §10 Public API & Framework Consumer Contract. Renumbered §10 Appendices to §11. Added Migration Safety & Rollback Policy to §4.5. |
| 0.2.01 | 2026-06-20 | Claude | Added §2.6 Future Features (Deferred Scope). Tightened §2.5 language. Cross-reference §9.3 to §2.6. |
| 0.2.02 | 2026-06-20 | Claude | Added §4.15 and §4.16 Deferred Implementation Alternatives. |
| 0.6.3 | 2026-06-29 | Claude | **v0.6.3 batch.** Header note updated to reference new file path `agents/exemplars/`. All internal cross-references to companion files updated to correct paths. File naming pattern in header updated to `[projectname]_architecture_NN_topic_vN.md`. V/R/M phase refs removed. CHANGELOG.md pointer added. |
| 0.7.0 | 2026-07-02 | Claude | **v0.7.0 batch.** §4.13 gains an Asset Manifest subsection (filename, type, repo target path, authoritative/informative-for, provided-at-step) per `CLAUDE.md` §3.8, with the requirement that every logged asset is referenced substantively elsewhere in §4.13, not left as a manifest-only entry. §9.1 Sequencing Principles gains the Frontend Targeted Interleaving principle (UI screen/component Build Order steps placed alongside their real backend data dependency, never before it and never batched into a trailing frontend-only step). No change to the file-split header note — it already deferred to `CLAUDE.md` §4, which now specifies the 8-file one-per-Design-Step split without requiring this template's own text to change. |
| 0.7.1 | 2026-07-04 | Claude | **Verbosity pass — no intended behavior change.** Prose throughout condensed (repeated rationale/phrasing tightened); every section, table (columns and example rows), mandate, and cross-reference preserved verbatim in substance — header count and table-row count identical to the pre-pass file. §4.13's Asset Manifest note updated to state explicitly that asset *files* are never carried in Design-Phase handoff notes (`CLAUDE.md` §3.8/§3.11) — only referenced by filename; the manifest table itself is unaffected. ~16% smaller by size. |
| 0.8.5 | 2026-07-04 | Claude | **Major compression pass** (874 → 412 lines): schema-only, every table/ID-format/mandate preserved verbatim, rationale prose condensed. Added an ESP32/ESP-IDF cross-reference (§4.9). Pre-compression version retained by the user separately for human reference. |
| 0.8.11 | 2026-07-09 | Claude | **v0.8.11 batch.** §4.13 gained an **Authority Level** column to the Asset Manifest (Authoritative / Authoritative-with-changes / Conceptual / Firm-layout-content-forthcoming / other user-stated label, overriding the type-based default) and an explicit requirement that every logged mockup gets a prose description here, not a manifest row alone; new note on Proactive UI-Impact Flagging (a later feature implying an unshown UI change is flagged, pending an updated mockup) and an Auth/Authorization UI subsection (sign-in/sign-up mockup description; role assignment/permission/revocation surface, cross-ref §4.8). §4.8 Security Architecture gained a Role Assignment/Permission/Revocation bullet. Appendix R's own description broadened to state explicitly that no frontmatter/header box of any kind belongs at the top of the file (`CLAUDE.md` §4.3) — all such metadata lives in this appendix alone. |
| 0.9.4 | 2026-07-19 | Claude | **v0.9.4 batch.** Two new appendices added ahead of Appendix R: **Appendix B — Bug Fix Log** (Track A, Tier 1/2 corrective bug fixes, numbered by shipping release version, append-only) and **Appendix F — Spec Amendments** (Track B, Tier 3 fixes and Future Feature promotions that add/modify/supersede a Requirement, including divergences discovered during ordinary Development's Final Phase, not only Maintenance Phase). Both are companions to the forthcoming `agents/MAINTENANCE.md`. §11's ToC line updated to list both. |

---

## agents/exemplars/development_plan_template.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.1.00 | — | — | Initial creation of complex/multi-phase variant. |
| 0.2.00 | — | — | Rust-only complex/multi-phase variant generalized. |
| 0.2.01 | 2026-06-20 | Claude | §0 project note updated; §15 DoD tightened. |
| 0.2.02 | 2026-06-24 | Claude | §11.3 Phase Summary formalized. §13.1 escalation path added. |
| 0.2.03 | 2026-06-24 | Claude | Phase Summary gained header block and Assumptions section. §13.1 split into (A) replanning and (B) re-architecting. |
| 0.6.3 | 2026-06-29 | Claude | **v0.6.3 batch.** §4 Missing-Tools: agent installs missing prerequisites idempotently and appends commands to scripts/setup_env.sh and scripts/setup_env.bat. §5 pre-flight version sanity check added. §6 One-Phase-Per-Session mandate in Phase Index. §8 README task template added. §11 Phase Summary naming updated to `[projectname]_phaseN_summary.md`. §12 No-Copy-Checklist mandate. Deploy/docker guidance in §5. File naming throughout updated to new pattern. V/R/M refs removed. CHANGELOG.md pointer added. |
| 0.7.0 | 2026-07-02 | Claude | **v0.7.0 batch.** §0 project note updated to cite the split Traceability location (`_04_test_strategy` + `_05_verified_traceability`, per `CLAUDE.md` §4.1) instead of a single §3; new Audit dependency note citing Design Step 9. §3 Project Folder Structure gains the `assets/{html,images,audio,video}/` convention — user-populated, filename-stable, immutable, cross-referenced against the Architecture Spec's Asset Manifest; missing asset file is now an explicit Escalation Trigger. §6 gains the Frontend Targeted Interleaving phase-sequencing mandate, cross-referencing the corresponding principle now in `architecture_specification_template.md` §9.1. |
| 0.7.1 | 2026-07-04 | Claude | **Verbosity pass — no intended behavior change.** Prose condensed throughout; every section, table, mandate, and cross-reference preserved verbatim in substance — header and table-row counts identical to the pre-pass file. §3's asset note extended to clarify the files themselves were never carried through Design-Phase handoff notes (`CLAUDE.md` §3.8/§3.11) — this directory is genuinely where the actual files first land, possibly updated relative to what was shown during Design. §15's filename list updated to `[projectname]_dev_agent_prompt.md`. |
| 0.8.5 | 2026-07-04 | Claude | **Major compression pass** (555 → 273 lines). §13 Escalation Triggers rewritten to Stop-Summarize-Wait. §6.1/§8: README now drafted at Design Step 8, not scaffolded here — final-phase task is review/finalization only. §15 filenames reverted to `[projectname]_dev_prompt.md`. Added ESP-IDF toolchain row to §4. |
| 0.8.6 | 2026-07-06 | Claude | **v0.8.6 batch.** §14 Change Control: added Ambiguity-Resolving Amendments rule — any amendment resolving an ambiguity where already-completed phases might not satisfy the clarified intent MUST add a verification task to the next dependent not-yet-started phase (check, correct, confirm via standard DoD/Verification-Method; never assumes compatibility). §3 Project Folder Structure: added required `.gitignore` specification — `/target`, `/dist` (Trunk projects), `.env` (with `.env.example` committed), and IDE/OS noise entries; cross-referenced `AGENTS.md` §2.1's corrected preservation mandate distinguishing committed evidence artifacts from gitignored build outputs. |
| 0.8.7 | 2026-07-07 | Claude | **v0.8.7 batch — work-preservation overhaul.** §6.1 Phase Index gained a **Session Unit** column (`Phase`/`Task`/`Code+Verify`). §8 Task Template gained mandatory **Design Refs** (item-level Spec citation, no version suffix, mandatory every task) and **Submit Point**/**WIP-Checkpoint-Eligible** fields; new **Mandatory Code/Verify Split** rule (`a`/`b` sub-tasks for any Build+Test/Hybrid Verification Method task, counts as 2 against Phase Sizing). §11 Session Handoff gained the three-way task-state check against declared Submit Points (not raw git-log). §12 Abort/Rollback scoped to reverting to a task's own last submitted state, not a phase-level checkpoint. §15 DoD gained checks for Design Refs completeness, mandatory split compliance, and Session Unit consistency. |
| 0.8.11 | 2026-07-09 | Claude | **v0.8.11 batch.** §6.1 Phase Index gained a mandatory **Complexity Score** column, computed and shown for every phase (never left blank or only implied by Task Count); over-ceiling requires a recorded override note in the same cell. §15 DoD gained a corresponding check. §6's Frontend Targeted Interleaving note extended: each frontend task's Design Refs now cite its mockup's prose description and logged Authority Level, and a task introducing an unshown page/view/setting traces back to the Design-Phase Proactive UI-Impact flag that justified it. Appendix R's own description broadened to state explicitly that no frontmatter/header box of any kind belongs at the top of the file (`CLAUDE.md` §4.3) — all such metadata lives in this appendix alone. |
| 0.8.12 | 2026-07-11 | Claude | **v0.8.12 batch.** §3's `.gitignore` paragraph rewritten: now drafted at **Design Step 8** (not a Development Phase 0 responsibility), reviewed/confirmed/extended at Development Phase 0, mirroring the README convention; mandatory skeleton entries changed from root-only (`/target`, `/dist`) to **depth-agnostic** (`**/target`, `**/dist`) so they match a Rust crate/component or Trunk build at any nesting depth, not just the repo root. §8 Task Template gained a **UI-touch rule**: any task adding/modifying/visibly changing a UI component, feature, or appearance must use Visual/Behavioral or Hybrid (never Build+Test alone) and must specify at least one screen capture; a **consolidation** rule allows one shared capture across multiple UI-touching tasks in the same phase only when all their changes are visible together in one screen/state — otherwise each requires its own capture. §11.4's "Build/test tasks" format bullet tightened: the Verification file must carry the actual terminal output line copied verbatim, never a paraphrase/description/restated claim (e.g. "Result: Successfully compiled using `cargo check -p [crate]`" is now an explicit violation, not an acceptable summary) — addresses an observed defect where agents wrote descriptive claims instead of real command output. |
| 0.9.0 | 2026-07-12 | Claude | **v0.9.0 batch.** §3 Project Folder Structure gained the `src/` vs `crates/` mutual-exclusivity rule (decided once at Design Step 5, recorded in the Architecture Spec's structure viewpoint) and new entries for `scripts/metrics/`, `metrics/*.toml` (CI-maintained, committed, not gitignored), `.github/workflows/ci.yml`, `deny.toml`, `LICENSE.md` (static text, final at Step 8), and `THIRD_PARTY_LICENSES.md` (CI-maintained, drift-checked). |
| 0.9.2 | 2026-07-16 | Claude | **v0.9.2 batch.** §3 gained a mechanical scaffold mandate: every member crate's (or, for a single-crate project, the root) generated `Cargo.toml` MUST include `publish = false` in its `[package]` table from the moment it's scaffolded — not a documentation reminder left to a separate file, since that pattern is exactly what produced an earlier real README-template gap. This is also what makes cargo-deny's new `[licenses.private]` (`PREFERRED_TOOLS.md`) correctly recognize the crate as private. §3's `.gitignore` skeleton gained a depth-agnostic `**/node_modules` entry, for projects with Playwright/frontend Node tooling (`PREFERRED_TOOLS.md`'s new Frontend Visual Testing section). |
| 0.9.4 | 2026-07-19 | Claude | **v0.9.4 batch.** §8 gained nine `PROD-NNN` task definitions (Regression Traceability, Versioning, Anti-Stub Final Sweep, README [= `DOC-FINAL`], Spec/Dev-Risks Currency, License/Dependency Drift, and three conditional tasks — Rollback Procedure, Operational Runbook, Monitoring Baseline), one per `agents/DEVELOPMENT.md` §5.2.4 Productization Readiness Checklist item. §15 gained two DoD lines: Final Phase must include applicable `PROD-NNN` tasks; every phase's Exit Criteria must cite the CI-equivalent-sequence-and-fix requirement. §6's intro gained a matching note. |

---

## agents/CI.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.9.0 | 2026-07-12 | Claude | **v0.9.0 batch.** New file. GitHub Actions CI stage skeleton, referenced from `AGENTS.md`, `README.md`'s repository structure list, and `agents/DESIGN.md` Steps 5/8. Fixed stage order: **Stage 0 (Environment Setup)** — `scripts/setup_env.sh` (with `~/.cargo/bin` caching) then `scripts/check_env.sh` as a fail-fast confirmation, mandatory ahead of every other stage since CI has no Environment-Snapshot equivalent and starts from zero state every run; Build; Test (nextest); Coverage (llvm-cov → `metrics/coverage.toml`); **Stage 5 (Security & License Scan)** — cargo-deny (licenses + advisories) and cargo-audit, feeding `metrics/deny.toml`/`metrics/audit.toml`, plus a drift check regenerating `THIRD_PARTY_LICENSES.md` from the current dependency tree and failing (not auto-committing) if it differs from the committed copy; conditional E2E (Playwright, `metrics/playwright.toml`) only when the project has such a suite; conditional infra-services startup only when `deploy/docker-compose.dev.yml` exists; Metrics commit (bot commit of updated `metrics/*.toml`, main-branch merges only). Includes a **"Reading a Stage 5 Failure"** note distinguishing the three failure signatures that can all surface as a Stage 5 red — cargo-deny license-check (`error[rejected]: failed to satisfy license requirements`, named crate/license), cargo-deny advisory-tier (`unmaintained`/`yanked`, different message shape), cargo-audit RustSec (CVE ID + advisory link) — since all three currently look identical from the stage name alone. Includes an explicit **Open Question** note: CI-failure triage criteria (what's worth human escalation vs. routine noise) are deliberately left to human judgment for now, pending accumulated real-project data, rather than codified as a rule. States explicitly that early-phase CI red is common and not itself alarming, but that persistence without narrowing across multiple phases, or an identical failure signature (e.g. same exit code) across otherwise-unrelated stages, is worth a human look — a tentative heuristic, not a hard rule. CI is documented as an async, human-reviewed backstop: no session-blocking gate, no Development Plan-level rule requiring the next task's session to wait on a prior push's CI result. **Also this batch — production fix from an observed real CI failure:** §3 gained a **Cross-Job Tool Persistence** note: GitHub Actions jobs run on separate, isolated runners, so a tool installed by Stage 0 in one job does not exist in a different job unless that job independently repeats the install — `~/.cargo/bin` caching alone does not bridge this. Observed failure: `cargo deny`/`cargo audit` reported as unknown subcommands in a Security & License Scan job because they were installed only in an earlier, separate job. §3's `on:` trigger explicitly specified as `push` only, never `pull_request` — consistent with §2's CI-as-async-backstop framing rather than a merge gate. Stage 0 gained a literal, copy-exact YAML snippet (checkout pinned to `actions/checkout@v5`, `setup_env.sh` then `check_env.sh`) since this exact sequence had been independently rediscovered by multiple agent sessions rather than reliably generated correctly from prose alone. Stage 5 rewritten to prefer `EmbarkStudios/cargo-deny-action@v2`/`actions-rust-lang/audit@v1` (self-installing, job-structure-independent) over raw `cargo deny`/`cargo audit` shell invocation as the actual pass/fail gate, with a same-job direct-invocation step retained only for `metrics/deny.toml`/`metrics/audit.toml` JSON extraction, explicitly not as the gate itself. §4.1 describes both resulting Step 9 mechanical checks. Stage 0's literal YAML snippet extended with a real `actions/cache@v5` step (`~/.cargo/bin`/registry/`target`, keyed on `Cargo.lock`) — `v4` was found to carry the identical Node-20-deprecation warning as `checkout@v4`, confirming this recurs per-action rather than being a checkout-specific fix; see `PREFERRED_TOOLS.md`'s now-generalized GitHub Actions pinning table. |
| 0.9.1 | 2026-07-14 | Claude | **v0.9.1 batch -- structural rewrite.** Stages 0-5 merged from separate jobs into one job's sequential steps, eliminating the cross-job tool-persistence failure mode by construction (a tool installed by Stage 0 is simply still present for every later step in the same job) rather than working around it stage-by-stage; Metrics Commit remains a genuinely separate job (different trigger condition). Runner standardized to `ubuntu-latest` throughout; checkout standardized to bare `actions/checkout@v5`, no inline comment. Stage 1 (Lint & Format) introduced as its own stage ahead of Build. Build split into 2a (Native) / 2b (WASM, conditional). Test restructured into 3a (non-infra) / 3b (infra-dependent, conditional) / 3c+ (E2E/Playwright, conditional, extensible) rather than one combined stage; doc-tests generalized to run for whichever crates have them, no hardcoded crate name. Stage 5 (Security Scan) reverted from the dedicated `EmbarkStudios/cargo-deny-action@v2`/`actions-rust-lang/audit@v1` GitHub Actions back to direct `cargo deny`/`cargo audit` shell invocations -- the dedicated Actions caused recurring issues in practice, and the cross-job justification for using them no longer applies now that Stage 0's install is shared within one job. New `metrics/source.toml` branch/commit/status marker, written as the main job's final step with `if: always()` so it captures fail states too, updated on every push (not gated to a clean run) to function as an at-a-glance personal dashboard on the main-branch README while phases are still in progress. Metrics-script guidance updated to Node.js-preferred/bash-acceptable/Python-forbidden, cross-referencing `PREFERRED_TOOLS.md`'s new standing mandate. |
| 0.9.2 | 2026-07-16 | Claude | **v0.9.2 batch — three structural fixes plus Playwright hard-gating.** Every step in the stage skeleton (lint/format, native/WASM build, nextest, doc-tests, Playwright install/test/metrics, coverage run/parse, cargo-deny/parse-deny/cargo-audit/parse-audit/parse-license-metrics/drift-check, branch-marker write, artifact upload) gained an explicit `id:` so its outcome is individually addressable. New final "Evaluate required gates" step added, checking every required step's `steps.<id>.outcome` and failing the job on any non-success — a single, clear failure point rather than relying on GitHub Actions' own step-failure propagation alone; placed after the branch-marker write and artifact upload, since both carry their own `if: always()` and must still run and capture state even when an earlier stage failed. **Stage 3c (Playwright) corrected to a hard gate**: the test-execution step (`npx playwright test`) now carries no `continue-on-error`/`\|\| true`, superseding an earlier (incorrect) non-fatal framing — non-fatal handling remains scoped to the one-time `setup_env.sh` install-guard block only (`PREFERRED_TOOLS.md`). CI-side browser install (`npx playwright install --deps`) made explicit as its own step, distinct from and required in addition to `setup_env.sh`'s guard. **Ordering bug fixed:** "Write branch/status marker" now runs *before* "Upload metrics artifact" (previously reversed, so the uploaded artifact never actually contained `metrics/source.toml` — it didn't exist yet at upload time); "Evaluate required gates" runs last. **`metrics_commit` job de-`main`-gated:** `if: always() && github.ref == 'refs/heads/main'` → `if: always()` (now runs on every branch, not `main` only); checkout gained an explicit `ref: ${{ github.ref_name }}` (bare checkout could leave a detached-HEAD workspace); `git push` → `git push origin HEAD:${{ github.ref_name }}` (a bare push from detached HEAD has no reliable target). Every surrounding prose sentence describing Metrics Commit, the `source.toml` badge, or Stage 6 as `main`-branch-specific reworded to branch-local/branch-uniform language — the settled design has every branch self-contained, with no cross-branch or `main`-specific commit logic anywhere in the pipeline. Stage Skeleton version label bumped to v0.9.2. |
| 0.9.4 | 2026-07-19 | Claude | **v0.9.4 batch.** `actions/upload-artifact`/`download-artifact` bumped from `@v4` to `@v5` in both occurrences (the `ci_pipeline` close-out steps and the `metrics_commit` job), matching `PREFERRED_TOOLS.md`'s pinning table which had already moved to `@v5` in v0.9.3 — this file's own literal yaml examples had drifted from that table. `metrics_commit` job gained an explicit `permissions: contents: write` block — without it, `git push` fails regardless of the job's other v0.9.1/v0.9.2 fixes being correct. Fix count in the accompanying explanatory note updated from "three fixes" to "four fixes." |
| 0.9.4 (revision) | 2026-07-19 | Claude | **Second ordering bug found and fixed in the same close-out section.** The v0.9.4 fix above moved `Write branch/status marker` to run before `Upload metrics artifact` (correctly fixing the artifact-inclusion bug) but left `Evaluate required gates` running last — this meant `job.status` at marker-write time was always `"success"`, since individual stage steps use `continue-on-error` and only `Evaluate required gates`'s own unguarded `exit 1` actually fails the job; `metrics/source.toml` could report `status = "pass"` on a run that genuinely failed. Corrected order: `Evaluate required gates` → `Write branch/status marker` → `Upload metrics artifact` — satisfies both constraints (accurate `job.status`, and the marker file still exists before the artifact is captured). Section retitled and both ordering notes rewritten to explain each bug and its fix in turn. |
| 0.9.4 (revision 2) | 2026-07-19 | Claude | **A third, related bug found: the prior revision's own rationale text ("individual stage steps use `continue-on-error`") was false against the actual literal YAML — no Stage 1–5 step set `continue-on-error: true` anywhere, and one paragraph explicitly (and incorrectly) said Playwright's test step should not have it either.** Without `continue-on-error`, the first failing step stops the job outright and skips everything after it — `Evaluate required gates` would then only ever observe one failure per run, defeating its own stated purpose of aggregating pass/fail into one clear failure point. Added `continue-on-error: true` to all 17 Stage 1–5 steps (`check_formatting`, `run_clippy`, `native_build`, `wasm_build`, `test_nextest`, `test_docs`, `playwright_install`, `playwright_test`, `playwright_metrics`, `coverage_run`, `coverage_metrics`, `cargo_deny`, `parse_deny`, `cargo_audit`, `parse_audit`, `parse_license_metrics`, `drift_check`) — deliberately excluding Stage 0 (a genuine prerequisite hard-stop; downstream output is meaningless if tool install fails) and `Evaluate required gates` itself (its failure must propagate, or it has no purpose). Discovered in the same pass that `check_formatting`/`run_clippy`/`native_build`/`wasm_build` were never in the gate-evaluation script's check list at all — added their `steps.<id>.outcome` lines so giving them `continue-on-error` doesn't silently swallow a lint/build failure instead of aggregating it (the two changes are coupled, documented as such). New "Continue-on-Error Policy" subsection added, right after Stage 5, stating this reasoning once rather than scattering it across per-stage prose. |

---

## agents/exemplars/development_checklist_template.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.6.3 | 2026-06-29 | Claude | **v0.6.3 batch (initial versioned entry).** How-to-use section: No-Copy-Checklist mandate added explicitly. README tasks added to Phase 0 and final phase. File naming updated. CHANGELOG.md pointer added. |
| 0.8.5 | 2026-07-04 | Claude | Compression pass (135 → 101 lines). Phase 0's `DOC-001` task reworded from "scaffold" to "review, confirm, and enhance" the Design-Step-8-drafted README. |
| 0.8.7 | 2026-07-07 | Claude | **v0.8.7 batch.** Added a **Submitted** checkbox per task/sub-task (checked only once its declared Submit Point actually fires, distinct from local DoD satisfaction). Added the Code/Verify `a`/`b` split rendering convention as two adjacent Task sub-sections, each with its own DoD and Submitted checkboxes; example `DOMAIN-002` task updated to demonstrate the split. |
| 0.8.8 | 2026-07-08 | Claude | **v0.8.8 batch.** How-to-use section gained an explicit **bracket-content-only** statement of scope (mirroring `AGENTS.md` §2.7): a Development Phase session may flip a mark in an existing `[ ]`, check a `Submitted` box, or append a Session Log row — never reword, comment on, restructure, or touch another phase's marks. "Required artifact captured" items now point at the phase's Verification file/folder (`test/[projectname]_phase_[N]_verification.md`, `test/phase_[N]/`) rather than an inline description. The `(repeat one \`## Phase N\` block per phase...)` placeholder is rewritten as an explicit instruction that the actual generated Checklist expands every phase/task in full — never delivered with a "repeat" note left in (`AGENTS.md` §2.3 No Compressed Formats); how-to-use section gained a matching bullet. |
| 0.8.12 | 2026-07-11 | Claude | **v0.8.12 batch.** How-to-use section gained a new bullet mandating exact Checklist order: a Development Phase session never reorders, skips ahead, or leaves a DoD sub-item unchecked-but-passed-over to move on, without the user's explicit permission given that session — an apparently unnecessary or already-satisfied task/item is a question or Escalation Trigger, never a silent skip. |
| 0.9.0 | 2026-07-12 | Claude | **v0.9.0 batch.** Phase 0's task list gained `DOC-002` (review/confirm/extend `ci.yml`, mirroring `DOC-001`'s README pattern), `DOC-003` (create `deny.toml`, review/confirm/extend `THIRD_PARTY_LICENSES.md`, plus running the first real `cargo deny check licenses` against the resolved `Cargo.lock` — the earliest point a transitive-dependency license violation is genuinely catchable), and `DOC-004` (confirm `LICENSE.md` — static text, a simple accuracy check rather than a reconciliation). |
| 0.9.3 | 2026-07-19 | Claude | **v0.9.3 batch.** How-to-Use section gained a bullet on a new recurring DoD item: every phase's Documentation/QA task carries a "README badge URLs point to this phase's Branch Name" item, not just first/final phases. `DOC-001` (Phase 0) and `DOC-FINAL` gained explicit badge-branch-and-org/repo-substitution DoD items; the illustrative `## Phase 1` block gained a new `DOC-PHASE-BADGE` task demonstrating the recurring per-phase pattern. |
| 0.9.4 | 2026-07-19 | Claude | **v0.9.4 batch.** Final Phase gained `PROD-001`, `PROD-002`, `PROD-003`, `PROD-005`, `PROD-006`, `PROD-007`, `PROD-008`, `PROD-009` checklist task blocks (`PROD-004` is satisfied by the existing `DOC-FINAL` task, noted rather than duplicated), one per `agents/DEVELOPMENT.md` §5.2.4 Productization Readiness Checklist item, mirroring `development_plan_template.md` §8's new task definitions. |

---

## agents/exemplars/dev_risks_template.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.1.0 | 2026-07-16 | Claude | **New file (v0.9.2 batch).** Same tier as `CHANGELOG_template.md`; generates `docs/[project_name]_dev_risks.md`, an append-only, edited-in-place living log for standing risks discovered during the Development Phase — `deny.toml` `[advisories].ignore` exceptions, `[[licenses.exceptions]]` entries, advisory-driven dependency swaps, and approved `[patch]` exceptions — deliberately kept separate from the Architecture Specification's Risk Management section, which covers only risks identified during Design and is governed by that document's own change-control. One entry template per risk: date discovered, RUSTSEC ID/crate+version, mechanism, full justification text (the durable counterpart to a terse inline `deny.toml` comment), re-evaluation trigger, and status. Generated as an initial empty/skeleton file at Design Step 8 (`agents/DESIGN.md` §5.8), alongside `CHANGELOG.md`. |

---

## agents/exemplars/dev_prompt_template.md (renamed from dev_agent_kickoff_prompt_template.md 0.8.0, then dev_agent_prompt_template.md 0.8.5)

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.6.3 | 2026-06-29 | Claude | **v0.6.3 batch (initial versioned entry).** §1 doc list updated to new file naming pattern. §2 environment check expanded: missing tools self-installed + appended to setup_env.sh/bat; version sanity check against project compatibility matrix. §4 One-Phase-Per-Session mandate added. §5 No-Copy-Checklist mandate added. Session-end checklist updated. V/R/M refs removed. CHANGELOG.md pointer added. |
| 0.7.0 | 2026-07-02 | Claude | Step 1's doc list updated to the 8-file, one-per-Design-Step Architecture Spec naming (`01_introduction` through `08_constraints_and_roadmap`), fixing a prior example that implied a single shared version placeholder across all Architecture files — each is now shown as independently versioned. Added Asset Manifest cross-check as doc-review item 5, including an explicit Escalation Trigger for a manifest-listed asset missing from the repository. "Notes for Whoever Fills In This Template" corrected to match. |
| 0.8.0 | 2026-07-03 | Claude | **File renamed** `dev_agent_kickoff_prompt_template.md` → `dev_agent_prompt_template.md`; output artifact renamed `[projectname]_dev_prompt.md` → `[projectname]_dev_agent_prompt.md` — updated everywhere referenced (`AGENTS.md`, `CLAUDE.md`, `agents/DESIGN.md`, `agents/DEVELOPMENT.md`, `README.md`, `agents/exemplars/development_plan_template.md`). Prompt text now opens with an explicit statement that the agent is operating in the Development Phase under `agents/DEVELOPMENT.md` (not Design Phase), directing the agent to read that guide if not already internalized this session. |
| 0.8.5 | 2026-07-04 | Claude | **File renamed back** to `dev_prompt_template.md`; output reverted to `[projectname]_dev_prompt.md`, per explicit user instruction reverting the 0.8.0 rename. Compression pass (245 → 112 lines). Session steps rewritten to Stop-Summarize-Wait: any unresolved issue stops the session immediately at the Phase Summary — no PR, no further progress. |
| 0.8.7 | 2026-07-06 | | Claude | **v0.8.7 batch — major restructure.** Step 1 rewritten from "read all docs upfront" to a two-tier model: mandatory-upfront (checklist, phase summaries, current phase §6.1+§8 via targeted extraction, protocols file in full) then on-demand only. New step 2: on-demand reference table mapping 17 uncertainty types (interface contract, data model, requirement wording, test case, traceability, concurrency/ownership, security, technology/MSRV, build order, architectural risk, user story, system overview, deployment, dev plan risk/test/environment/stack) to specific `[projectname]_architecture_NN` or `_dev_plan_NN` file + section + `sed`/`grep`/`awk` extraction command — consulted only when confusion, conflict, missing clarity, or abnormality arises during task work. CRITICAL context window rules block added at top of step 1: no broad repository scan, no whole-file reads of large documents (protocols file excepted), all doc access via targeted extraction. Former steps 2–9 renumbered 3–10. Session-End Checklist gains verification item for the no-scan/no-whole-file-read rules. Notes section updated with fill-in guidance for extraction commands and reference table placeholders. |
| 0.8.7 | 2026-07-07 | Claude | **v0.8.7 batch (part 2) — work-preservation overhaul.** Prompt text opens with Code Review Policy / Agent Tool Policy / post-submit Continue-Proceed / Session Unit cross-references. Step 6 ("find the next unit") gained the three-way task-state check against declared Submit Points, resuming in-place from a WIP checkpoint's own description rather than redoing work. Step 7 gained mandatory Code/Verify split handling and immediate per-Submit-Point submission instructions. Step 9 reframed as phase-level wrap-up submit only, generalized to Session Unit completion. Session-End Checklist gained items verifying every completed task/sub-task has a matching submit and Submitted checkbox, WIP checkpoint description sufficiency, and no code-review calls. |
| 0.8.8 | 2026-07-08 | Claude | **v0.8.8 batch.** New step 3, "Check out the phase branch," inserted before the environment check — steps renumbered 3–10 → 4–11 accordingly (all internal cross-references updated). Step 8 ("work the unit") gained the Verification-file-append instruction, run the instant a task's DoD is satisfied, before the Checklist update and submit. Step 9 (Phase Summary) now links to the Verification file rather than repeating its content. Reference table gained a row for Verification-file format / Branch Name convention. Session-End Checklist gained items for Verification-file completeness, working on the correct branch, and the bracket-content-only Checklist-edit rule (replacing the looser "only doc file you edited" wording). |
| 0.8.12 | 2026-07-11 | Claude | **v0.8.12 batch.** Step 3 rewritten to require re-verifying the checked-out branch before every task, not just once at session start; never work on another phase's branch. Step 8 rewritten: new opening paragraph mandating exact Checklist task order (no reordering, no skipping ahead, no omitting a DoD sub-item without explicit user permission that session); Verification-file append instruction now requires pasting the actual terminal output line verbatim (explicit anti-example: a paraphrase like "Result: Successfully compiled using `cargo check -p [crate]`" is called out as a violation); Checklist update instruction tightened to "before calling `submit`... do not defer... do not batch"; **`Submitted` checkbox timing fixed** — it is no longer checked when flipping the Checklist boxes pre-submit, but only after `submit` completes and the user responds "Continue"/"Proceed". Session-End Checklist gained a per-task branch-re-verification item and a Checklist-order item; the existing Submitted-box item now states the same only-after-resume timing rule. |
| 0.9.3 | 2026-07-19 | Claude | **v0.9.3 batch.** Step 3 gained an explicit exact-Branch-Name mandate: use the Plan's declared Branch Name verbatim, character-for-character — never append a numeric hash, timestamp, or session identifier (observed failure examples: `phase12b-4334437132416834814`, `phase11a_transcode_backend_codec-5155833834103153101`). |

---

## README.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.7.0 | 2026-07-02 | Claude | Initial versioned entry. Updated Design Phase description from 8-step to 9-step gated workflow (Step 9 Plan & Checklist Audit added). Key Mandates gained "Independent audits" and "Tracked UI/media assets" bullets to reflect the same batch of changes landing across `CLAUDE.md`/`DESIGN.md`/the exemplar templates. |
| 0.7.1 | 2026-07-03 | Claude | "Tracked UI/media assets" bullet clarified: asset files are never packaged into a Design Phase handoff note, only referenced by filename. New "Development agents write to one file" bullet added, reflecting `AGENTS.md` §2.7's extended exclusive-file-touch/phase-scoped-marks mandate. Repository tree entry renamed to `dev_agent_prompt_template.md`. |
| 0.8.5 | 2026-07-04 | Claude | **v0.8.5 batch.** Key Mandates rewritten: fixed RCD/RATS procedure and Surgical Fix Override description replace the autonomy-toggle framing; added the Stop-Summarize-Wait escalation model; added the Design-Step-8-drafts-README note; added the ESP32/ESP-IDF build guide to the repository tree and Key Mandates. Tightened the gated-execution bullet to state exact `APPROVED` (no synonyms). |
| 0.8.8 | 2026-07-08 | Claude | **v0.8.8 batch.** New opening line and "Workflow for Humans" section: states explicitly that Design Phase sessions are Claude-based and Development Phase sessions are Jules-based, and walks through the end-to-end human-facing process (Design Phase step-by-step, Step 8/9 handoff package, per-session Jules workflow including branch checkout/Session Unit/Submit Points, reviewing the Verification file and Phase Summary, looping back to a Design Phase/Claude session on escalation). Key Mandates gained bullets for per-phase Branch Name, per-task submit cadence, and the per-phase Verification file; the "Development agents write to one file" bullet tightened to state the bracket-content-only edit scope explicitly. |
| 0.8.11 | 2026-07-09 | Claude | **v0.8.11 batch.** "Independent audits" Key Mandate rewritten: findings now tag Trivial or Substantive; an all-Trivial result is fixed in place, same session, no backtrack; a Substantive finding still requires the full workflow or the renamed **Post-Audit Fix Pass** (formerly Surgical Fix Override), which fixes every finding plus anything discovered incidentally, never leaving a defect flagged-but-unfixed. |
| 0.9.0 | 2026-07-12 | Claude | **v0.9.0 batch.** Repository Structure tree gained `agents/CI.md` and `agents/exemplars/README_template.md`, with a new note clarifying this tree describes the `grail` instruction repo itself, not a generated project's structure (which has its own `metrics/`, `deploy/`, etc., per `development_plan_template.md` §3). Step 8's handoff-package description (Workflow for Humans, step 3) extended to include draft `ci.yml` and draft `THIRD_PARTY_LICENSES.md`. Key Mandates gained a new bullet on CI as an async, human-reviewed backstop (no session-blocking gate). |

---

## agents/ESP32_ESPIDF_RUST_BUILD_GUIDE.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.8.5 | 2026-07-04 | Claude | New file, added to `agents/` — user-supplied ESP32/ESP-IDF/Rust/tokio build guide, scoped to embedded projects only, cross-referenced from `CLAUDE.md` §2.1, `agents/RUST_PREFERENCES.md`, and both `PREFERRED_DEPENDENCIES.md`/`PREFERRED_TOOLS.md` Embedded sections. |
| Phase 0 patch | 2026-07-18 | Claude | Patch: Version Reference table `espup` row pinned to `0.14.1` (was unpinned `latest`), per Phase 0 Escalation `fesp32_phase0_summary.md` — an unpinned install had resolved a pre-Rust-2024-edition nightly toolchain. Quick-Start Checklist gained a matching verification item. |
| 0.9.3 | 2026-07-19 | Claude | **v0.9.3 batch.** New patch note added: any tool in this guide's Version Reference table that publishes a prebuilt binary release for the environment's platform should prefer that binary over `cargo install`, consistent with `PREFERRED_TOOLS.md`'s new binary-install-preference mandate — falls back to the guide's existing pinned `cargo install --version X --locked` form only when no binary release exists or a tool's correctness depends on building against the exact local toolchain. |
| 0.9.4 | 2026-07-19 | Claude | **v0.9.4 batch.** `espflash` install form updated to `cargo binstall espflash`, per `PREFERRED_TOOLS.md`'s sharpened binary-preference mandate (prefer `cargo binstall` specifically over plain `cargo install` for any cargo-installable tool), falling back to `cargo install espflash --locked` only if no binary is published for the platform. `espup` noted as not currently benefiting from this (no known prebuilt binary), so its pinned `cargo install --version --locked` form is unchanged. |

---

## agents/exemplars/README_template.md

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.9.0 | 2026-07-12 | Claude | **Full rewrite.** Replaced the prior generic/Node-TS-flavored placeholder content with this framework's actual Rust/GitHub Actions/shields.io conventions: badge row rebuilt on shields.io's dynamic-toml-badge, pointed at `metrics/*.toml` on the repo's default branch (coverage, tests, deny, audit, playwright); Tech Stack table replaced with the Rust toolchain and `PREFERRED_DEPENDENCIES.md`/`PREFERRED_TOOLS.md`/`PREFERRED_SERVICES.md` categories actually in use; Prerequisites/Installation sections rewritten around `rustup`/`cargo`/`scripts/setup_env.sh` instead of Node/npm; File Hierarchy block rewritten to the standardized `src/`-or-`crates/` (mutually exclusive), `assets/`, `deploy/`, `metrics/`, `scripts/`, `test/`, `.github/workflows/` layout; License section rewritten for the PolyForm Noncommercial default (source-available, free for personal/noncommercial use, commercial use requires a separate license from the author) in place of the prior Apache-2.0 badge/section. |
| 0.9.3 | 2026-07-19 | Claude | **v0.9.3 batch.** Badge URLs' branch segment changed from a hardcoded `main` to a `[current_branch]` placeholder — metrics commit to whichever branch CI last ran on, not to `main`. Metrics & Badges section gained a note explaining the placeholder and stating that every phase's Phase-End Quality Assurance (`agents/DEVELOPMENT.md` §5.2), not just Design time, re-checks and updates it to the current phase's Branch Name, alongside re-confirming `[org]`/`[repo]` substitution. |
| 0.9.4 | 2026-07-19 | Claude | **v0.9.4 batch.** Repository Layout gained `test/[projectname]_requirement_traceability.md`, the Requirement-to-Test traceability artifact scaffolded at Design Step 8 and maintained incrementally through Development (`agents/DEVELOPMENT.md` §5.2.4 item 1). |

---
